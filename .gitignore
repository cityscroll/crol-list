node_modules/
.DS_Store
__pycache__/
.generated/
docs/data-source-graph.json
docs/data-source-graph.html

# Agency constellation HTML (build/deploy artifact only).
# Built by tools/build_agency_constellation_documents.mjs during
# tools/build_cloudflare_pages.mjs — never commit per-agency pages.
# Keep site/agencies/index.html (directory listing) tracked.
site/agencies/*/index.html

# Local notes and planning dirs (not part of the product tree)
# Internal-only planning locations. Gitignore prevents future adds, but CI is
# the enforcement layer for tracked-path introductions in PRs.
internal/
tasks/
backlog/
*.local.md

# Local editor / agent helper files
# AGENTS.md is committed (standing project contracts for contributors).
CLAUDE.md
PAPERCUTS.md
.claude/
.agents/
# Local browser performance results
test/performance/artifacts/
.artifacts/
.external-schedule-state/

# Cloudflare Wrangler local cache (never commit)
.wrangler/

# CityScroll warehouse bulk data (WH-01) — code/fixtures stay tracked; bulk never in git
warehouse/raw/**
!warehouse/raw/.gitkeep
# Public-records captures are deliberately committed with their provenance;
# bulk source downloads remain ignored by the rule above.
!warehouse/raw/public-records/
!warehouse/raw/public-records/**
warehouse/parquet/**
!warehouse/parquet/.gitkeep
warehouse/duckdb/**
!warehouse/duckdb/.gitkeep
warehouse/receipts/**
!warehouse/receipts/.gitkeep
!warehouse/receipts/proof/
!warehouse/receipts/proof/**
!warehouse/receipts/public-records/
!warehouse/receipts/public-records/**
warehouse/.venv/
entity_resolution/eval/.venv/
warehouse/.ingest.lock
warehouse/**/__pycache__/
*.duckdb
*.duckdb.wal

# Generated WH-02 bulk samples (may contain public notice fields; not committed)
warehouse/fixtures/**/bulk_sample.csv
# Independent enacted-law mandate pipeline: raw statute caches and private
# comparison artifacts must never enter the public repository.
tools/law_mandates/cache/
tools/law_mandates/output/
tools/law_mandates/private/
